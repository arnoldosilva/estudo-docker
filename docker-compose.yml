version: '3.9'
services:
  web:
    build: .
    ports:
      - "4000:80"
    volumes:
      - ./:/app/node_modules
      - ./:/app
  tests:
    build:
      context: .
      dockerfile: "dockerfile.dev"
    volumes:
      - ./:/app/node_modules
      - ./:/app
    command: ["npm","run","test"]

